{% extends "base.njk" %}
{% block title %}{{ conference.name }} – ResearchRoomies{% endblock %}
{% block content %}
<h2>{{ conference.name }}</h2>
<p>{{ conference.description }}</p>
<ul>
  {% for post in posts %}
  <li><a href="/post/{{ post.id }}">{{ post.title }}</a></li>
  {% endfor %}
</ul>
{% endblock %}